/** Tamanho único da página do feed — lista, hasMore e “Carregar mais”. */
export const PAGE_SIZE = 40;
/** Overfetch para clusterizar sem partir a mesma história no limite da página. */
export const FEED_CLUSTER_FETCH = PAGE_SIZE * 2;
