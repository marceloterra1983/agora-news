story_id: e03s02
title: Cutover PM2 para Compose na 3080
status: failing
bcps: 5
risk: P0
security: medium
tasks:
  - id: 1
    description: "Build da imagem no host (PM2 ainda no ar)"
    verify: "docker compose -f /home/marce/news/compose.yml build"
    risk: P0
    status: failing
    allure:
      severity: critical
      categories: ["infra", "Security Review"]
  - id: 2
    description: "Preflight da imagem em 127.0.0.1:3081"
    verify: "curl -sS -o /dev/null -w '%{http_code}' --max-time 15 http://127.0.0.1:3081/fontes?secao=ai | grep -qx 200"
    risk: P0
    status: failing
    allure:
      severity: critical
      categories: ["smoke"]
  - id: 3
    description: "Cutover 3080 + smoke local e vivo"
    verify: "curl -sS -o /dev/null -w '%{http_code}' --max-time 15 http://127.0.0.1:3080/fontes?secao=ai | grep -qx 200 && curl -sS -o /dev/null -w '%{http_code}' --max-time 20 https://news.automatizems.com/fontes?secao=ai | grep -qx 200"
    risk: P0
    status: failing
    allure:
      severity: critical
      categories: ["smoke", "Security Review"]
  - id: 4
    description: "PM2 news não está online"
    verify: "pm2 show news 2>/dev/null | grep -qi 'status.*stopped' || pm2 show news 2>&1 | grep -qi 'doesn.t exist\\|not found\\|erased'"
    risk: P1
    status: failing
    allure:
      severity: high
      categories: ["ops"]
