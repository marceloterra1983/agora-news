# Opcional. Se definido, /api/ingest exige Authorization: Bearer <valor>.
# Sem isto, o cron da Vercel e clientes sem Sec-Fetch-Site continuam aceitos.
CRON_SECRET=

# Auth / DB (já usados pelo template)
# DATABASE_URL=
# BETTER_AUTH_URL=
# VITE_AUTH_ENABLED=

# Cache (Upstash / Vercel KV)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
